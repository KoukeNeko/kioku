SERVER_ADDR=:8090

# Irodori-TTS-Server on the GPU host.
IRODORI_BASE_URL=http://192.168.50.169:8088
IRODORI_API_KEY=replace-with-irodori-secret

# Bearer token required from the Nekogo app. Use a different secret from IRODORI_API_KEY.
APP_API_KEY=replace-with-app-secret

CACHE_DIR=./data/audio-cache
# Optional JSON object mapping entry_id to a human-reviewed pronunciation string.
TTS_OVERRIDES_FILE=./tts-overrides.json
DEFAULT_VOICE=dictionary-ja-01
APPROVED_VOICES=dictionary-ja-01,none

IRODORI_MODEL_NAME=irodori-tts
MODEL_REVISION=Irodori-TTS-500M-v3
VOICE_VERSION=v1
PROFILE_VERSION=quality-v1
IRODORI_NUM_STEPS=60
IRODORI_REQUEST_TIMEOUT=5m
MAX_CONCURRENT_SYNTHESIS=1
RATE_LIMIT_PER_MINUTE=60
MAX_AUDIO_MIB=16
